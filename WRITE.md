# ✍️ 快捷创作入口使用指南

## 🎯 功能说明

这是一个**自动发布系统**，让您可以直接将 Markdown 文件放入创作文件夹，系统会自动处理并发布。

## 🚀 快速开始

### 1. 启动自动发布监听

```bash
pnpm write
```

### 2. 开始创作

将您的 Markdown 文件直接放入 `src/content/drafts/` 文件夹即可！

## 📝 使用示例

### 方式一：没有 frontmatter 的文件

创建一个简单的 Markdown 文件 `我的文章.md`：

```markdown
# 我的文章标题

这是文章内容...
```

放入 `src/content/drafts/` 后，系统会自动：
- ✅ 提取文件名作为标题
- ✅ 添加当前日期作为发布日期
- ✅ 生成完整的 frontmatter
- ✅ 移动到发布文件夹
- ✅ 删除源文件

### 方式二：已有 frontmatter 的文件

创建包含部分 frontmatter 的文件：

```markdown
---
title: "我的技术分享"
tags: [技术, 分享]
category: 技术
---

# 我的技术分享

这是文章内容...
```

系统会自动：
- ✅ 保留您已有的字段
- ✅ 补充缺失的必需字段（published）
- ✅ 设置默认值（draft: false）
- ✅ 移动到发布文件夹

## 📋 Frontmatter 字段说明

| 字段 | 必需 | 说明 | 默认值 |
|------|------|------|--------|
| `title` | ✅ | 文章标题 | 从文件名提取 |
| `published` | ✅ | 发布日期 | 当前日期 |
| `description` | ❌ | 文章描述 | 空字符串 |
| `image` | ❌ | 封面图片路径 | 空字符串 |
| `tags` | ❌ | 标签数组 | 空数组 |
| `category` | ❌ | 分类 | 空字符串 |
| `draft` | ❌ | 是否为草稿 | false |
| `lang` | ❌ | 语言代码 | 空字符串 |

## 💡 工作流程建议

### 推荐流程

1. **启动开发服务器**（终端 1）：
   ```bash
   pnpm dev
   ```

2. **启动自动发布监听**（终端 2）：
   ```bash
   pnpm write
   ```

3. **开始创作**：
   - 在 `src/content/drafts/` 文件夹中创建/编辑 Markdown 文件
   - 文件保存后自动处理并发布
   - 在浏览器中实时查看效果

### 单独使用

如果只需要快速发布文件，也可以：
1. 将文件放入 `src/content/drafts/`
2. 运行 `pnpm write` 处理一次
3. 按 Ctrl+C 退出

## ⚠️ 注意事项

1. **文件已存在**：如果目标文件已存在，脚本会跳过处理（避免覆盖）
2. **文件格式**：只处理 `.md` 和 `.mdx` 文件
3. **文件删除**：处理完成后，源文件会自动从创作文件夹删除
4. **实时监听**：脚本会持续监听文件夹，直到手动停止（Ctrl+C）

## 🔧 故障排除

### 文件没有被处理？

1. 检查文件扩展名是否为 `.md` 或 `.mdx`
2. 确认文件在 `src/content/drafts/` 文件夹中
3. 检查目标文件夹 `src/content/posts/` 中是否已存在同名文件

### 想要保留源文件？

目前脚本会自动删除源文件。如果需要保留，可以：
1. 手动复制文件到创作文件夹
2. 或者修改脚本逻辑（在 `scripts/auto-publish.js` 中注释掉删除代码）

## 📁 文件夹结构

```
blog/
├── src/
│   └── content/
│       ├── drafts/          ← 创作文件夹（放入文件）
│       │   ├── README.md
│       │   └── .gitkeep
│       └── posts/           ← 发布文件夹（自动生成）
│           └── ...
└── scripts/
    └── auto-publish.js      ← 自动发布脚本
```

---

**提示**：您也可以使用 `pnpm new-post <文件名>` 创建新文章，但使用创作文件夹更加灵活快捷！

